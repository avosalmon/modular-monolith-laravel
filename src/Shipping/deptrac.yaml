parameters:
  paths:
    - ./src/Shipping
  layers:
    - name: ShippingModule
      collectors:
        - type: bool
          must:
            - type: className
              regex: Shipping\\.*
          must_not:
            - type: className
              regex: Shipping\\Contracts\\.*
    - name: App
      collectors:
        - type: className
          regex: App\\.*
    - name: Contract
      collectors:
        - type: directory
          regex: src/[^/]*/Contracts/.*
    - name: Illuminate
      collectors:
        - type: className
          regex: Illuminate\\.*
  ruleset:
    ShippingModule:
      - App
      - Contract
      - Illuminate
